# Portability Analysis Report — Configure Failed

**Status:** cmake configure FAILED (exit code 1)  
**Generated at:** 2026-05-26T23:05:48Z  
**Tool:** analyze-portability v1.1.0  

---

## Configure Failure Details

| Field | Value |
|-------|-------|
| Source dir | `/home/user/workspace/portability_stress_projects/directshow_camera` |
| Build dir  | `/tmp/anaport_y1BNTR` |
| Exit code  | 1 |

**Error:** CMake File API reply dir not found: /tmp/anaport_y1BNTR/.cmake/api/v1/reply
  Did cmake configure succeed?

## What This Means for Portability Analysis

CMake configure failed before producing the File API reply. Because the File API reply is the primary data source for this tool, a full portability analysis could not be completed.

This typically happens when the project has **platform-specific CMake dependencies** that cannot be satisfied on the current OS. Examples:

- Windows-only packages required unconditionally (`find_package(DirectX REQUIRED)`)
- Windows SDK paths hard-coded in CMake scripts
- FetchContent downloading Windows-only binaries
- MSVC-only compiler flags set without platform guards

## Data Sources Status

| Source | Available |
|--------|-----------|
| CMake File API reply | **NO** — configure failed |
| compile_commands.json | no |
| CMakeCache.txt | yes — 130 variables (partial configure) |

## Recommendations

1. **Inspect the configure log** — check `configure_or_tool.log` (if saved with `-v`) for the cmake error messages.
2. **Check platform-specific CMake dependencies** — look for `find_package`, `FetchContent`, and Windows SDK paths used without `if(WIN32)` guards.
3. **Run on the supported OS** — if the project targets Windows only, run analyze-portability in a Windows environment to get a full report.
4. **Add platform guards** — add `if(WIN32)` / `if(APPLE)` / `if(UNIX)` conditions to CMakeLists.txt to make configure succeed on all platforms (even if features are disabled).
5. **Abstract platform-specific dependencies** — use CMake's cross-platform mechanisms (`find_library`, `find_package` with `QUIET` + fallback) rather than hard-coding SDK paths.

---

*Generated by analyze-portability v1.1.0 — configure failure diagnostic mode.*
